[Unit]
Description=FRKN Business Dashboard
After=network.target

[Service]
Type=simple
User=dashboard
Group=dashboard
ExecStart=/opt/dashboard/dashboard /opt/dashboard/config.toml
Restart=on-failure
RestartSec=5
WorkingDirectory=/opt/dashboard

[Install]
WantedBy=multi-user.target
