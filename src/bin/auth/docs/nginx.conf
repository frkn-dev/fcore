upstream auth {
    server 127.0.0.1:3000;
}

server {
    listen 443 ssl http2;
    server_name auth.frkn.org;
    server_tokens off;

    ssl_certificate /etc/letsencrypt/live/auth.frkn.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/auth.frkn.org/privkey.pem;

    location / {
        proxy_pass http://auth;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Alternatively, expose auth under the API domain:
#
# location /auth {
#     proxy_pass http://auth;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
# }
