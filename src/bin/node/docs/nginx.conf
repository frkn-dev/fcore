# The node agent does not expose an HTTP server.
# It connects outbound to the API and ZMQ endpoints configured in node-example.toml.
# Therefore no nginx reverse proxy is required.
#
# If you want to expose node stats externally, run a separate exporter
# or proxy the optional Prometheus/metrics endpoint that the node may publish via ZMQ.
